# Dart / Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/

# The android/ platform scaffold is committed. Other native platforms
# (linux/, windows/, …) are still generated locally via `flutter create .`
# until we decide to commit them.

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Dart pin file is regenerated by `flutter pub get`; CI resolves fresh.
pubspec.lock
